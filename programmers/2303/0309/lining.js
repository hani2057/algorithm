function solution(n, k) {
  const arr = new Array(n).fill().map((_, idx) => idx + 1);
  console.log(arr);

  // while (result.length < k) {

  // }
  // const permutation = (num, arr, k) => {
  //   const result = [];

  //   while (result.length < k) {
  //     // nPk에서 k가 1인 경우 원소를 그대로 배열에 담에 반환
  //     if (num === 1) return arr.map(el => [el])

  //     // k가 2 이상일 경우
  //     for
  //   }
  //   return result[k]
  // }
}

console.log(solution(3, 5)); // [3,1,2]
